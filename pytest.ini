[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: fast isolated tests, no network, no LLM
    integration: multi-component tests using mock scenario files
    eval: LLM-as-Judge tests that call Ollama — run deliberately
